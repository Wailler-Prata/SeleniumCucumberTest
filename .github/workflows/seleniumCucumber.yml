name: Selenium Cucumber Tests

on: push

jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'  # Use a versão adequada do Node.js

      - name: Install Dependencies
        run: npm install

      - name: Baixar e configurar o Microsoft Edge WebDriver
        run: |
          wget https://msedgedriver.azureedge.net/116.0.1938.54/edgedriver_linux64.zip
          unzip msedgedriver_linux64.zip
          chmod +x msedgedriver
          sudo mv msedgedriver /usr/local/bin/

      - name: Run Tests
        run: npm run run:test:cucumber

      - name: generate report
        if: ${{always()}}
        run: npm run generate:MultipleHtml:report

        
      - name: Deploy report page
        uses: peaceiris/actions-gh-pages@v3
        # If you're changing the branch from main,
        # also change the `main` in `refs/heads/main`
        # below accordingly.
        if: ${{ always() && github.ref == 'refs/heads/main' }}
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./report